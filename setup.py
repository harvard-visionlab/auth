from setuptools import setup

setup(
    name='visionlab.auth',
    version='0.1.0',
    packages=['visionlab.auth'],
    package_dir={'visionlab.auth': 'auth'},
    namespace_packages=['visionlab'],
    python_requires='>=3.3',
    # Additional metadata (classifiers, author info, etc.)
)